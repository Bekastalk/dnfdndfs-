spring:
    datasource:
        password: root
        url: jdbc:mysql://localhost:3306/root
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        database-platform: org.hibernate.dialect.MySQLDialect
server:
    port: 9090